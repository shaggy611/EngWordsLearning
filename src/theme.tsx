import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    mode: 'dark',
  },
  typography: {
    button: {
      fontSize: '1.2rem',
    },
  },
})

export default theme
